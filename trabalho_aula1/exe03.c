#include <string.h>
#include <stdio.h>
#include <stdlib.h>

/*
void troca(){
int strcpy(char *origem, char *destino){
   int i = 0;
   while (*(origem + i) != '\0'){
      *(destino + i) = *(origem + i);
      i++;
   }
   *(destino + i) = '\0';
   return i;
}

*/

int main(){
   char frase[100];
   printf("Digite uma frase:\n");
   printf("%s", frase);
   printf("A Fjrase é: %c\n",frase);
   return 0;
}